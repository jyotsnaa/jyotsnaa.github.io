(function($,Drupal){'use strict';Drupal.behaviors.yamlFormDetailsToggle={attach:function(context){$('.js-yamlform-details-toggle',context).once('yamlform-details-toggle').each(function(){var $form=$(this);var $details=$form.find('details');if($details.length<2){return;}
$details.first().before($('<button type="button" class="link yamlform-details-toggle-state"></button>').attr('title',Drupal.t('Toggle details widget state.')).on('click',function(e){var open;if(isFormDetailsOpen($form)){$form.find('details').removeAttr('open');open=0;}
else{$form.find('details').attr('open','open');open=1;}
setDetailsToggleLabel($form);if(Drupal.yamlFormDetailsSaveGetName){$form.find('details').each(function(){var name=Drupal.yamlFormDetailsSaveGetName($(this));if(name){localStorage.setItem(name,open);}});}}).wrap('<div class="yamlform-details-toggle-state-wrapper"></div>').parent());setDetailsToggleLabel($form);});}};function isFormDetailsOpen($form){return($form.find('details[open]').length==$form.find('details').length)}
function setDetailsToggleLabel($form){var label=(isFormDetailsOpen($form))?Drupal.t('Collapse all'):Drupal.t('Expand all');$form.find('.yamlform-details-toggle-state').html(label);}})(jQuery,Drupal);